# Implementation Plans

Generated by the improve skill on 2026-06-11 at commit `3a0b685`. Execute in the order below unless dependencies say otherwise. Each executor should read the target plan fully before starting, honor its STOP conditions, and update the status row when done.

This plan set came from a deep advisory audit. Source files were not modified while writing these plans.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| [001](001-seed-check-snapshots.md) | Seed snapshots during pre-edit checks | P1 | M | - | DONE |
| [002](002-release-pg-advisory-locks.md) | Always release Postgres advisory locks during initialization | P1 | S | - | DONE |
| [003](003-support-ts-namespace-imports.md) | Resolve TypeScript namespace-import dependency edges | P1 | M | - | DONE |
| [004](004-harden-daemon-local-inputs.md) | Harden local daemon JSON and server-message parsing | P2 | S | - | DONE |
| [005](005-refresh-docs-roadmap.md) | Refresh roadmap and status documentation | P2 | S | - | DONE |
| [006](006-publish-live-branch-updates.md) | Publish branch changes during active sessions | P2 | M | - | DONE |
| [007](007-reduce-check-hot-path-scans.md) | Avoid full source-tree scans on warm pre-edit checks | P2 | L | 001 recommended | DONE |
| [008](008-extract-verification-harness.md) | Extract a shared verification-script harness | P3 | M | 001, 003, 004 recommended | DONE |
| [009](009-design-state-delta-broadcast.md) | Write the design for incremental `state.delta` broadcast (D3) | P2 | M | - | DONE (design doc at docs/design/state-delta-broadcast.md; owner review gates implementation) |
| [010](010-publish-npm-front-door.md) | Make the published npm package the install front door | P1 | S | - | DONE (staged 0.2.0; `npm publish` handed to owner per plan) |
| [011](011-spike-onboard-briefing.md) | Spike `synapse onboard` first-session briefing (C4 slice) | P2 | M | - | DONE |
| [012](012-spike-rename-tracking.md) | Spike TypeScript rename tracking (`renamed` deltas, F5 slice) | P3 | M | - | DONE (renamed deltas keep the OLD symbol id — deliberate divergence from the plan so dependency_changed keeps firing; see PR) |
| [013](013-pr-decision-memory.md) | Carry distilled PR-thread prose into repo events + vector memory (C3 slice) | P2 | M | - | DONE |
| [014](014-synapse-demo-command.md) | Build `synapse demo` — one-command sandboxed conflict demo | P2 | M | 010 pairs well | DONE |
| [015](015-detection-quality-benchmark.md) | Detection-quality benchmark — per-rule precision/recall ratchet | P3 | M | 012 bonus, not required | DONE (25 scenarios; `dependency_changed` baselined at 0.833 precision — a real, documented FP on compatible 1-hop dependency changes) |
| [016](016-command-grounded-llm-actions.md) | Ground the LLM layer in Synapse's command catalog (suggested actions) | P2 | M | - | DONE |
| 017 | Catalog-grounded agent guidance for `synapse connect` | P2 | M | 016 | DONE |
| 018 | Bind local services to loopback by default | P1 | S | - | DONE |
| 019 | Constrain daemon file reads to the worktree | P1 | M | 018 | DONE |
| 020 | Pin GitHub webhook repo ids to the signed payload | P1 | S | - | DONE |
| 021 | Clear failed state-load retry cache entries | P1 | S | - | DONE |
| 022 | Honor `synapse session --action` regardless of argument order | P2 | S | - | DONE |
| 023 | Preserve TypeScript barrel re-export symbol identity | P1 | S | 003 | DONE |
| 024 | Make Go analyzer failures visible in CI and package checks | P1 | S | - | DONE |
| 025 | Time out and recover stuck analyzer sidecar requests | P1 | M | 024 | DONE |
| 026 | Align documented and packaged Node runtime floor | P1 | S | 010 | DONE |
| 027 | Consolidate package verification on the release tarball path | P2 | S | 026 | DONE |
| 028 | Implement protocol v2 `state.delta` broadcast | P2 | M | 009, protocol negotiation | DONE |
| 029 | Expose Synapse context through MCP resources | P2 | S | 017 | DONE |
| 030 | Add local aggregate `synapse insights` / `synapse_insights` | P2 | S | 017 | DONE |
| 031 | Add local PR handoff briefing | P2 | M | 020, 029 | DONE |
| [035](035-strict-pr-quality-gates.md) | Add strict GitHub PR quality gates | P1 | L | - | DONE (PR #84; branch `advisor/035-strict-pr-quality-gates`; local strict gates green after merge, package gate required temp npm cache because the global npm cache has root-owned files) |
| [036](036-atomic-server-authoritative-check.md) | Make `synapse_check` register intent and read peer locks in one atomic server round-trip | P2 | L | - | MERGED — [PR #86](https://github.com/Kumario1/synapse/pull/86), merge commit `6781b81` on `main`. Executed in isolated worktree, reviewed & APPROVED; reviewer re-ran all criteria green (typecheck/build/`npm test` 0 failures across 8 workspaces; `verify:atomic-intent` confirmed bob's intent ack carries alice's lock with no report; `verify:delta-broadcast` + `verify:protocol-compat` regress clean). Includes one post-review simplification commit (ponytail: inlined the lock union since the engine already dedups conflicts; relocated `peerLocksForIntent` to `state.ts` to drop a test-only server export — net −26 lines). Rebased onto `origin/main` (which already had 035) before merge — zero conflicts, combined tree re-verified green. |
| [037](037-web-dashboard.md) | Add a web dashboard + landing page (`apps/web`) visualizing a live Synapse room | P2 | L | - | DONE (reviewed & APPROVED; `apps/web` Vite/React dashboard added with seeded demo + live WebSocket feed, type-only protocol imports, no server/package/turbo source changes; reviewer re-ran web/root typecheck, build, tests, no-fetch check, out-of-scope diff checks, and browser smoke on desktop/mobile) |
| [038](038-gate-state-prune-sweeps.md) | Gate the per-read state prune sweeps behind a short interval | P2 | S | - | TODO |
| [039](039-stop-refreshing-ended-session-lastseen.md) | Stop refreshing `lastSeen` on ended sessions so they actually prune | P2 | S | - | TODO |
| [040](040-rate-limit-state-and-recall-routes.md) | Rate-limit the `/state` and `/recall` HTTP routes | P2 | S | - | TODO |
| [041](041-close-ci-strict-gate-coverage.md) | Pull the race-prone verifiers into the required CI gate and guard against drift | P2 | M | - | TODO |
| [042](042-add-eslint-prettier-baseline.md) | Add ESLint + Prettier with type-aware async-hazard rules | P2 | M | - | TODO |
| [043](043-bind-webhooks-to-signed-repo-in-tenancy-mode.md) | Strictly bind GitHub webhooks to the signed payload's repo in tenancy mode | P2 | M | - | TODO |
| [044](044-cap-edit-locks-per-session.md) | Cap edit locks per session to bound state growth | P3 | S | - | TODO |
| [045](045-spike-auto-maintained-contract-surface.md) | Auto-maintained contract surface as an MCP resource | P2 | M | - | TODO |
| [046](046-spike-close-the-loop-fix-handoff.md) | Close-the-loop — structured fix handoff to the downstream agent | P2 | M-L | - | TODO — the **deterministic** fix-handoff spike (design at `docs/design/fix-handoff.md`). The LLM-resolution direction it deliberately deferred is now scoped by **PRD [#109](https://github.com/Kumario1/synapse/issues/109)** + **ADR-0002** (issues #110–#114); `FixHandoff` is the deterministic floor those build on. |
| [047](047-github-professional-standards-docs.md) | Add AGENTS.md + contributor/community-health docs (professional GitHub standards) | P1 | M | - | DONE (executed in isolated worktree, reviewed & APPROVED 2026-06-16). Plan was **reconciled** mid-execution: first run correctly STOPPED because `main` had advanced past the plan's `259e3f7` baseline to `ba1bd003` and plan 042 had landed, adding `lint`/`format` scripts — invalidating the "no lint script" premise. Plan refreshed to `main`/`ba1bd003` (lint/format now referenced; lint is part of the `static`→`required` gate), then re-executed clean. 8 net-new files (AGENTS.md, CONTRIBUTING.md, SECURITY.md, CODEOWNERS, .github/PULL_REQUEST_TEMPLATE.md + 3 ISSUE_TEMPLATE/*), 217 insertions, zero source touched. Worktree branch `advisor/047-github-standards-docs`, commit `c219715` — **not pushed; merge is the owner's call.** Note: AGENTS.md states "`main` is protected" which becomes true only once plan 048 runs. |
| [048](048-repo-governance-protect-main-and-clean-branches.md) | **Owner-run runbook** — protect `main`, auto-delete merged branches, clean stale branches & worktrees | P1 | M | 047 (rec.) | TODO — **owner must run** (not executor-eligible; every phase mutates the GitHub remote or local git). Facts re-verified 2026-06-16 @ `ba1bd003`: still unprotected, auto-delete off, 47 remote branches, 0 open PRs. Read-only dry-run done: Phase 3a would delete **46** merged-PR branches (`main` safe), Phase 3b keep-list is now **empty** (its 7 branches all gained merged PRs), 27 worktrees / 21 leftover `/private/tmp`. Ready for the owner to execute phase-by-phase. |
| [049](049-web-accounts-landing-dashboard.md) | Expand the web surface — marketing landing, login-gated onboarding, account-bound live dashboard | P2 | XL | - | DONE (issue #98; migrated `apps/web` to Next.js 15 + Auth.js v5, added GitHub-gated onboarding/dashboard, account-scoped connection metadata persistence, and local-only server Token handling; owner still must provision OAuth/Neon and switch Vercel preset). **⚠ Superseded by ADR-0001 (2026-06-17):** the hosted/GitHub-only-ownership pivot withdrew this self-hosted, no-token-custody, Auth.js direction and the implementation was removed (see PR #108, "SaaS auth ADR"). |
| [050](050-register-github-app-secrets.md) | Wire Synapse GitHub App secrets into the server | P1 | M | - | DONE (issue #102; merged as [PR #116](https://github.com/Kumario1/synapse/pull/116), squash commit `9b221f6` on `main`. Provides `loadGitHubAppConfig` + `SYNAPSE_GITHUB_APP_*` env boundary; no OAuth/install routes yet — those land in 051+) |
| [051](051-github-signin-end-to-end.md) | Sign in with GitHub end-to-end (human cookie-session boundary) | P1 | M | 050 | DONE (issue #103; executed in worktree, reviewed & APPROVED 2026-06-17, commit `9869b11` on `feat/github-signin`. GitHub App user-to-server OAuth + signed httpOnly cookie session, `users` table, `/auth/github` + callback + `/auth/me` + logout, auth-aware web topbar. Stateless signed cookie, session HMAC key derived from `clientSecret` (no new env var), signed+constant-time CSRF `state`. Boundary held distinct from the daemon credential per ADR-0001 — `authorized()`/WS/`/state`/webhook untouched. Gates green: server 85 tests pass (26 new auth), web typecheck/test/build, lint, format:check all 0. One justified out-of-scope tweak: `apps/server/package.json` test runner `dist/*.test.js`→`dist` so the new `auth/` subdir tests actually run.) |
| [052](052-claim-repo-via-app-install.md) | Claim one repo by installing the GitHub App (Owner↔Project ownership + project-key) | P1 | M-L | 051 | DONE (issue #104; executed in worktree, reviewed & APPROVED 2026-06-17, commit `704ca17` on `feat/claim-project`. `projects(owner_id,repo_id,project_key)` store + `/auth/projects/add` → `/auth/github/setup` → `/auth/projects`. Setup callback exchanges the install-time OAuth code for the user token (used once, discarded), lists installation repos, claims only push-access repos, mints `deriveProjectKey(masterSecret,repoId)` idempotently per (owner,repo). Webhooks flow automatically via existing `/webhooks/github` — NO new webhook code, NO App-JWT minting. New optional env `SYNAPSE_GITHUB_APP_SLUG`. Gates green: server 100 tests (15 new), web typecheck/test/build, lint, format:check all 0; `handleGitHubWebhook`/`authorized()`/WS/`/state` untouched.) |
| [053](053-onboarding-connect-command.md) | Onboarding — show the connect command for a claimed Project | P1 | S-M | 052 | DONE (issue #105; executed in worktree, reviewed & APPROVED 2026-06-17, commit `dec77ad` on `feat/onboarding-connect`. Web-only: `Onboarding.tsx` lists the Owner's claimed Projects with the CLI install command + per-Project `SYNAPSE_PROJECT_KEY=<key> synapse up --server <wss> --repo-id <repoId>` start command (verified against CLI `config.ts` flag resolution) and a live connected `Badge` polling the existing `/state?repoId=&token=` route. `onboarding.test.ts` pins the exact command string as a CLI-rename tripwire. NO server/CLI/protocol change (`git diff -- apps/server apps/cli packages` empty). Gates green: web typecheck/test (20)/build, lint, format:check all 0.) |
| [054](054-dashboard-list-projects-render-room.md) | Dashboard — list claimed Projects and render the selected one's live Room | P1 | M | 052 | DONE (issue #106; executed in worktree, reviewed & APPROVED 2026-06-17, commit on `feat/dashboard-projects`. Cookie-authed, ownership-scoped `GET /auth/projects/state?repoId=` route (401 no session / 403 not_owner via `projectStore.getProject` / 200 owner) + `readRoomState` injected from index.ts (`withRepo`+`getState`). `ProjectsDashboard` lists the Owner's claimed Projects and polls the selected one's live Room (2s) into the existing `Dashboard` panels. Distinct from the machine `/state` (project-key) path; WS/`verifyClient`/`authorized()`/webhook untouched. Documented deviation: local `emptyRoomState` helper instead of the protocol value (importing it drags node:crypto into the browser bundle) — mirrors all 8 TeamState fields, web typecheck confirms. Gates green: server 104 tests (4 new), web typecheck/test (22)/build, lint, format:check all 0.) |
| [055](055-kick-agent-session.md) | Kick an agent Session from a Project's Room (Owner-authorized, HTTP) | P1 | M | 054 | DONE (issue #107; executed in worktree, reviewed & APPROVED 2026-06-17, commit `2ad5e1d` on `feat/kick-session`. Cookie-authed, ownership-scoped `POST /auth/projects/kick?repoId=&sessionId=` reusing the existing `session.end` teardown (end + clear files + release locks + broadcast) and closing the kicked daemon's socket via a new socket↔session WeakMap. HTTP only — no browser WS message (protocol unchanged), no ban. Per-session Kick button via optional `onKick` on `OnlinePanel`. Authz tests assert non-owner/401/400 never invoke the kick; state test asserts end releases locks + fresh join is active. Gates green: server 109 tests (5 new), web typecheck/test (23)/build, lint, format:check all 0; `authorized()`/WS/webhook/`/state` untouched.) |
| [056](056-mediator-tracer-two-phase-happy-path.md) | Resolution mediator — deterministic proposal + two-phase happy path (tracer) | P1 | L | - | DONE (issue #110; PR #122 green. Server-hosted mediator: contested `edit.intent` (peer lock revealed, plan 036) → deterministic mechanical `ResolutionProposal` (keep/adapt directions + call-site list read from `unpushedDeltas` before/after/dependents) stored transiently on `TeamState.resolutionProposals[]` + broadcast as a snapshot; new `resolution.ack` message → both-accept flips to `resolved`. No LLM, no persistence, no reject/timeout/semantic (those are #111/#112/#113). conflict-engine gains the pure call-site derivation; `synapse_resolution` MCP tool surfaces the Direction; `scripts/verify-mediator.mjs` proves the happy path.) |
| [057](057-mediator-reject-timeout-void-escalation.md) | Mediator — reject + timeout void path with Owner escalation | P1 | M | 056 | DONE (issue #111; executed in worktree, reviewed & APPROVED 2026-06-18, commit `b2364c0` on `feat/mediator-void`. `resolution.ack` `accept` widened to boolean; added `voided` status + `voidReason` (rejected/timeout) + `voidedBy`. Reject from either side OR TTL timeout (`SYNAPSE_RESOLUTION_TTL_MS`, server-side timer cleared on terminal) voids the pair → voided proposal is the Owner escalation in the broadcast; reject records dismiss `ConflictFeedback` via existing `appendFeedback` (no new store). `applyResolutionAck` tightened to act only on `resolving` — no partial commit (test proves ack-after-void is a no-op). Verifier extended with reject (separate symbol) + timeout (short-TTL server child) cases. Gates green: protocol 24 / server 117 tests, build/typecheck, verify:mediator (resolve+reject+timeout), lint, format:check all 0.) |
| [058](058-mediator-mechanical-vs-semantic-classification.md) | Mediator — mechanical-vs-semantic classification + keep/adapt roles | P1 | M-L | 056, 057 | DONE (issue #112; PR #124, commit `d414a25` on `feat/mediator-classify`. conflict-engine `classifyCollision`; protocol widened to mechanical/semantic + `awaiting_owner`/`candidates`; semantic proposals carry no fabricated `after`, await Owner pick via cookie-authed `POST /auth/projects/resolve-winner`, then `applyWinnerChoice` enters the existing keep/adapt resolving flow. No LLM, no persistence. Gates green: build/typecheck, conflict-engine 46 / protocol 25 / server 126 tests, `verify:mediator` (resolve+reject+semantic+timeout), lint, format:check all 0.) |
| [059](059-mediator-llm-adapt-prose.md) | Mediator — LLM-authored adapt prose without verdict changes | P1 | M-L | 056, 057, 058 | DONE (issue #113; planned at `f2fde2f`. Adds optional, injectable mediator prose enrichment for `adapt` direction summaries only. Deterministic proposal class/status/signatures/call-sites remain authoritative; no provider keeps current deterministic directions; OpenRouter provider is server-side, no raw source, disabled by `SYNAPSE_LLM_RESOLVE=0`.) |
| [060](060-mediator-owner-visibility.md) | Mediator — Owner visibility in insights and dashboard | P1 | M | 056, 057, 058 | DONE (issue #114; planned at `e28b7d5`. Adds mediator proposal counts to `synapse insights` and a compact Owner dashboard card for resolving/resolved/escalated proposals, with winner-choice buttons reusing the existing Owner-authenticated HTTP route. Visibility/decision only; no code-edit action. Gates green: build/typecheck, protocol 25 / cli 31 / web 25 / server 134 tests, web build, `verify:insights`, `verify:mediator`, lint, format:check, diff check all 0.) |
| [061](061-session-join-active-region-awareness.md) | Surface teammates' live edit regions at session start | P1 | S | - | DONE (issue #128; first warn-only Reservation slice. Uses existing `sessions` + `editLocks` in `synapse_whatsup`; no persisted Reservation and no `deny` behavior. Local gates green: build, CLI tests, `verify:session-start`, typecheck, lint, root `npm test` under Node 20.) |
| [062](062-persist-session-reservations.md) | Persist per-session Reservations from reported edits | P1 | M | 061 | DONE (issue #129; persisted/queryable Reservation state from reported edits, read by SessionStart. Local gates green: build, typecheck, lint, protocol/server/CLI tests, `verify:session-start`, root `npm test` under Node 20 + Python 3.12. No deny behavior and no dashboard UI.) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency Notes

- Plan 001 should land before Plan 007 if possible because both touch daemon check/report behavior and Plan 007 must preserve the pre-check snapshot invariant.
- Plan 008 is intentionally last because it refactors verifier scripts that Plans 001, 003, and 004 may extend. Running it after those reduces merge conflicts.
- Plans 002, 003, 004, 005, and 006 are otherwise independent.
- Plans 009–012 came from a separate direction audit (`improve next`, 2026-06-11, commit `8c46a61`) — owner selected all four. They are mutually independent and independent of 001–008, with one soft note: 009 is a design document only (the D3 implementation plans get written after the owner reviews it), and if 006 or 007 land first, 009's executor must re-check its cited broadcast-site line numbers (its drift check covers this).
- Recommended order for the new set: 010 (cheapest, highest adoption leverage) → 009 → 011 → 012.
- Plans 013–015 came from a second-tier direction audit later the same day (same commit `8c46a61`). All three are independent of each other and of 001–012, with soft pairings: 014 (`synapse demo`) is most valuable after 010 publishes the package; 015's corpus should gain a rename scenario if 012 lands first (its drift note covers this); 013 makes plan 011's onboarding briefing richer automatically once both land.
- Plans 013 and 014 both touch nothing in `apps/cli/src/daemon.ts`, so the in-flight plan-004 drift noted in 009/011/012 does not affect them (013's wire-schema overlap is noted in its own drift section).
- Plan 016 (owner-requested via `improve plan`, commit `8c46a61`) grounds the LLM analysis prompts in a catalog of Synapse's MCP tools — suggestions only, never execution, deterministic floor included, LLM stays optional. Independent of all others; if 011 lands first, add its `synapse_onboard` tool to 016's catalog (016's maintenance note covers this). 016's appendix scopes a deferred Phase-2 (agentic tool-calling) — that needs its own plan and owner sign-off before anyone builds it.
- **Review pass (2026-06-11, post PR #52):** all TODO plans (005–016) were re-reviewed — 009–016 cold-read by a fresh-context agent, 005–008 re-vetted against the live tree. Fixed: stale "in-flight plan 004" drift notes (004 merged as PR #52, `776a717`); plan 012's wrong symbol name (`buildSymbol`, not `toCodeSymbol`) and test count; plan 014's scope/done-criteria contradiction (up.ts export) and fictional env var (`SYNAPSE_DB_PATH` is the real knob); plan 016's gating ambiguity (validation is NEVER env-gated); plan 015's corpus/baseline schemas and scoring edge cases now fully specified; plan 005 narrowed (README:54 already fixed); plans 006/007 re-anchored to post-merge line numbers. Recommended execution order across all TODO plans: 010 → 006 → 007 → 009 → 011 → 012 → 013 → 014 → 016 → 015 → 005 → 008 (005 late because every plan edits README; 008 last because new verify scripts keep landing).
- **Execution pass (2026-06-12 to 2026-06-13):** plans 017–031 landed on `main`. The sequence hardened local boundaries first (loopback binds, worktree path safety, signed webhook repo binding, state-load retry recovery), then closed CLI/analyzer/release gaps (session action parsing, TS barrel identity, Go verification, sidecar timeouts, Node/package verification), then shipped the agent-facing surfaces (protocol v2 deltas, MCP resources, local insights, PR handoff briefing).
- **Follow-up after plan 031:** commit `d9c82b4` failed CI in `verify:pr-brief` because the verifier used a local repo override while the signed webhook payload still named `acme/widgets`. Commit `7e9409c` fixed the fixture by making the payload `repository.full_name` match the local repo binding; production behavior remains strict payload-bound repository validation.
- Plan 035 adds strict PR quality gates: fail-closed detection ratchets, test inventory, a strict agent-loop verifier, and split GitHub Actions jobs with one required aggregate check. It was merged with `origin/main` before PR creation so it includes the already-landed 017-034 work.
- Plan 037 (owner-requested via `improve plan`, commit `6781b81`, 2026-06-15) builds a web UI — a new `apps/web` Vite+React SPA (deployable to Vercel) with a marketing landing page plus a live dashboard: who's online, edit-signals/locks, an animated daemon↔server↔symbol data-flow graph, and a commits/PRs feed. It is a **read-only consumer of the existing server** — the server already sends full `state.snapshot` over WebSocket to v1 (no-`v`) clients, so the plan requires **zero** changes to `apps/server`/`packages` (an explicit STOP condition). Defaults to a seeded looping demo so a public deploy always looks alive; `?server=wss://…` switches to a real room. This **reverses** the earlier "D4/M13 web dashboard — not planned" decision below (the owner explicitly requested it; the lost landing-page source is recreated lean from README copy + the old page's fonts/tokens, not recovered). Independent of all other plans.
- Plans 038–046 were renumbered after a concurrent writer claimed 037 for the web dashboard; the later advisory set yielded to keep numbering monotonic and avoid duplicate plan ids.
- Plan 036 (owner-requested via `improve plan`, commit `27f5bb7`, 2026-06-15) closes the time-of-check-to-time-of-use race in the edit-intent path: `synapse_check` currently sends `edit.intent` fire-and-forget then evaluates against its async local mirror, so two simultaneous checks can each miss the other's lock. It reuses the existing `sendAck`/`forId` request-reply primitive to return the server-authoritative peer locks on the `edit.intent` ack, linearized by the server's per-repo `withRepo` apply. Additive wire change (optional `locks` on the ack — no `PROTOCOL_VERSION` bump), with a tight timeout fallback so a check never blocks. Independent of all TODO/DONE plans; complements 034 (the *write* coordination channel) and builds on already-merged 028 (protocol v2 deltas) and 032 (edit-lock TTL/liveness). It is a residual-window narrowing, not a mutex — see the plan's maintenance notes.
- **Resolution mediator (2026-06-17, owner-requested):** PRD [#109](https://github.com/Kumario1/synapse/issues/109), vertical-slice issues #110–#114, decision record `docs/adr/0002-llm-resolution-mediator-suggest-only.md`. It builds on the deterministic fix-handoff spike (046) and the atomic intent round-trip (036). ADR-0002 is now **Accepted** (2026-06-18) and the slices shipped as plans 056–060 (PRs #122–#126, all DONE): the server-hosted mediator opens a suggest-only `keep`/`adapt` two-phase proposal at the contested `edit.intent`, classifies mechanical vs semantic collisions, escalates semantic ones to the Owner (`awaiting_owner` → `POST /auth/projects/resolve-winner`), optionally lets the LLM rephrase only the losing side's `adapt` prose, and surfaces counts in `synapse insights` + the Owner dashboard. README and the technical spec describe this shipped behavior.
- Plan 059 (issue #113) follows 058 because it must enrich only the already-decided `keep`/`adapt` roles. It must not choose semantic winners; Owner visibility remains issue #114.
- Plan 060 (issue #114) is the final PRD #109 slice: visibility and Owner decision in existing surfaces only. It depends on 058's semantic `awaiting_owner` state and existing resolve-winner HTTP route.
- Plan 061 (issue #128) is independent of the mediator sequence. It intentionally ships only session-start awareness over active edit locks; persisted Reservations and PreToolUse `deny` stay future work.
- Plan 062 (issue #129) depends on 061 because SessionStart already has a warn-only rendering path to switch from raw locks to stored Reservations. It intentionally excludes issue #130's deny behavior and issue #131's dashboard UI.

## Verification Baseline

Read-only checks run during the audit:

- `npm audit --omit=dev --json` reported `0` production vulnerabilities.
- No-emit TypeScript checks passed for `packages/protocol`, `packages/conflict-engine`, `packages/analyzer-ts`, `apps/server`, and `apps/cli` using `tsc --noEmit` with build info redirected outside the repo.

Full build/test was not run during the advisory pass because it writes build artifacts; individual plans list the exact verification commands their executors should run.

## Findings Considered And Rejected

- Optional LLM resolver sends full file context when configured: documented in README privacy notes and gated by environment; not treated as a bug.
- Server auth accepts query-string tokens for backward compatibility: documented and lower priority than daemon hardening.
- Negative `/recall` limits and byte-vs-character payload counting: real edge cases but lower leverage than selected plans.
- Untracked `Synapse/` directory: appears generated/build-only and outside the tracked source audit; not planned here.

From the direction audit (2026-06-11):

- E1 VS Code extension / E4 editor rules: skipped — PR #35's `synapse connect` already delivers MCP registration + rules files to VS Code/Cursor/Windsurf; a native extension serves human UI, not agents, and adds a marketplace maintenance surface. Revisit on user demand.
- D4 / M13 web dashboard: ~~not planned~~ → **now planned as [037](037-web-dashboard.md)** (owner reversed this on 2026-06-15). Original rationale: `Synapse/` at the repo root holds only build output (`dist/` + `node_modules/`, no source), so folding it into `apps/web` first needed the lost landing source, and the vision doc deferred dashboards until revenue. Plan 037 sidesteps the lost source by recreating a lean landing from README copy + the old page's fonts/tokens, and ships the dashboard as a zero-server-change read-only consumer.
- M14 GitHub OAuth: stays decision-gated per D1 — its trigger is a hosted SaaS launch (a business event), not a code condition.

From the second-tier direction audit (2026-06-11, later the same day):

- Slack ingestion (C3's other half): vision doc explicitly defers it until revenue; needs new infrastructure. Not planned.
- More languages / SCIP: demand-driven; no user signal yet. Not planned.
- Dogfood Synapse on Synapse development: offered (this repo has no `.synapse/` despite concurrent agents working it — an executor and an advisor edited/cited `daemon.ts` simultaneously on 2026-06-11); owner declined for now. Cheap to revisit: `synapse join` + a daemon during dev sessions.
